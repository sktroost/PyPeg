---------------

-wie funktioniert coverage.py (tool für tests, zeigt was getestet wird) (DONE! thanks cfbolz!)

-relabel tests (parse tests anpassen, hinzufügen das neue labels passen müssen)
-statevariablen in vm.py sinnvoll umbenennen
-any, choice, commit bytecode (DONE! ->vm.py)
-alle tests die ich habe brauchen n testcase wo der string unerwartet früh zu ende is (z.b. pattern "ab" input "a", oder pattern "a" input "")
-getesteten prozentsatz des codes (insbesondere parser) erhöhen (siehe coverage und das htmlding)

python -m coverage run  `which py.test`
python -m coverage html
